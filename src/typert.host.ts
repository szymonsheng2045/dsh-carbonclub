import { remoteInvocations } from './remote-contract.js'

export const TYPERT = {
  package: 'dsh-human-buffer', face: 'host', schemas: [], invocations: remoteInvocations,
  model: { services: [], events: [], objects: [] },
} as const
export default TYPERT
