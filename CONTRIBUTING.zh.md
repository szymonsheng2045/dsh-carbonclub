# 参与贡献

请使用 Node 22.19+ 与 pnpm 11。运行 `pnpm install`，提交改动前执行 `pnpm check`。

协议变更必须同步更新 `docs/PROTOCOL.md`、补充对抗测试，并在破坏线协议兼容时升级版本化 GossipSub topic。会所消息绝不能进入 DSH 会话、模型请求、记忆、转录、分析载荷或第三方审核 API。

安全相关改动需要覆盖畸形输入、重放、时钟过期、资源上限和多节点测试。中继节点必须可替换，并且不得持有账号、历史记录或审核权。
